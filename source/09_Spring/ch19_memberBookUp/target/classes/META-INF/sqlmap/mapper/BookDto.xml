<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.lec.ch19.dao.BookDao">
    	<resultMap type="BookDto" id="book">
    		<result property="bnum" column="bnum"/>
    		<result property="btitle" column="btitle"/>
    		<result property="bwriter" column="bwriter"/>
    		<result property="brdate" column="brdate"/>
    		<result property="bimg1" column="bimg1"/>
    		<result property="bimg2" column="bimg2"/>
    		<result property="binfo" column="binfo"/> 
    	</resultMap>
    	
    	<select id="mainList" parameterType="BookDto" resultMap="book">
    		<![CDATA[
    			
    			SELECT * FROM BOOK ORDER BY BRDATE DESC
    		
    		]]>
    	</select> 
    	
    	<select id="bookList" parameterType="BookDto" resultMap="book">
    		<if test="schItem=='' or schItem == null">
    		SELECT * FROM
    		(SELECT ROWNUM RN, A.* FROM
    		(SELECT * FROM BOOK ORDER BY btitle)A )
    		WHERE RN BETWEEN #{startRow} AND #{endRow}
    		</if>
    		<if test="schItem=='all'">
    		SELECT * FROM
    		(SELECT ROWNUM RN, A.* FROM
    		(SELECT * FROM BOOK 
    			WHERE BTITLE LIKE '%'||#{schWord}||'%' or
    				  BWRITER LIKE '%'||#{schWord}||'%'
    			ORDER BY btitle)A )
    		WHERE RN BETWEEN #{startRow} AND #{endRow}
    		</if>
    		<if test="schItem=='btitle'">
    		SELECT * FROM
    		(SELECT ROWNUM RN, A.* FROM
    		(SELECT * FROM BOOK 
    			WHERE BTITLE LIKE '%'||#{schWord}||'%'
    			ORDER BY btitle)A )
    		WHERE RN BETWEEN #{startRow} AND #{endRow}
    		</if>
    		<if test="schItem=='bwriter'">
    		SELECT * FROM
    		(SELECT ROWNUM RN, A.* FROM
    		(SELECT * FROM BOOK 
    			WHERE BWRITER LIKE '%'||#{schWord}||'%'
    			ORDER BY btitle)A )
    		WHERE RN BETWEEN #{startRow} AND #{endRow}
    		</if>
    	</select>
    	
    	<select id="totCntBook" parameterType="BookDto" resultType="int">
    		
    		<if test="schItem=='' or schItem == null">
    		select count(*) cnt from book
    		</if>
    		<if test="schItem=='all'">
    		select count(*) cnt from book  WHERE BTITLE LIKE '%'||#{schWord, jdbcType = VARCHAR}||'%' or BWRITER LIKE '%'||#{schWord}||'%'
    		</if>
    		<if test="schItem=='btitle'">
    		select count(*) cnt from book  WHERE BTITLE LIKE '%'||#{schWord, jdbcType = VARCHAR}||'%' or BWRITER LIKE '%'||#{schWord}||'%'
    		</if>
    		<if test="schItem=='bwriter'">
    		select count(*) cnt from book WHERE BWRITER LIKE '%'||#{schWord}||'%'
    		</if>
    	</select>
    	<select id="getDetailBook" parameterType="int" resultType="BookDto">
    		select * from book where bnum = #{bnum}
    	</select> 
    	
    	<insert id="registerBook" parameterType="BookDto">
		<if test="bimg1=='' and bimg2!=''">
			INSERT INTO BOOK (BNUM, BTITLE, BWRITER, BRDATE, BIMG2, BINFO) VALUES
				(BOOK_SQ.NEXTVAL,  #{btitle},#{bwriter}, sysdate, #{bimg2},#{binfo})
		</if>
		<if test="bimg1!='' and bimg2==''">
			INSERT INTO BOOK (BNUM, BTITLE, BWRITER, BRDATE, BIMG1, BINFO) VALUES
				(BOOK_SQ.NEXTVAL,  #{btitle},#{bwriter}, sysdate, #{bimg1},#{binfo})
		</if>
		<if test="bimg1=='' and bimg2==''">
			INSERT INTO BOOK (BNUM, BTITLE, BWRITER, BRDATE, BINFO) VALUES
				(BOOK_SQ.NEXTVAL, #{btitle},#{bwriter}, sysdate, #{binfo})
		</if>
		<if test="bimg1!='' and bimg2!=''">
			INSERT INTO BOOK (BNUM, BTITLE, BWRITER, BRDATE,  BIMG1, BIMG2, BINFO) VALUES
			(BOOK_SQ.NEXTVAL, #{btitle},#{bwriter}, sysdate, #{bimg1},#{bimg2},#{binfo})
		</if>
	</insert>
	
	<update id="modifyBook" parameterType="BookDto">
		UPDATE BOOK SET
				BTITLE=#{btitle},
				BWRITER=#{bwriter},
				BRDATE=#{brdate},
				<if test="bimg1 != null and bimg1!=''">BIMG1=#{bimg1},</if>
				<if test="bimg2 != null and bimg2!=''">BIMG2=#{bimg2},</if>
				BINFO=#{binfo}
			WHERE BNUM=#{bnum}
	</update>
    
    </mapper>