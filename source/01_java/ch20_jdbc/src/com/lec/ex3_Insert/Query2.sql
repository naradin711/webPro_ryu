-- 부서번호 중복체크
SELECT * FROM DEPT WHERE DEPTNO = 70; -- RS.NEXT 결과가 TRUE면 중복 / FALSE면 부서명, 부서 위치 입력받아서 입력
SELECT COUNT(*) CNT FROM DEPT WHERE DEPTNO = 70;
-- 이제 입력
INSERT INTO DEPT VALUES (50, 'IT', 'SEOUL');



DELETE FROM DEPT WHERE deptno IN (50,60) ;

SELECT * FROM DEPT;

COMMIT;

UPDATE DEPT SET DNAME = 'BIGDATA' WHERE DEPTNO = 50;

INSERT INTO DEPT VALUES (55, 'IT', 'SEOUL');

UPDATE DEPT SET DNAME = 'FAKER', LOC = 'T1' WHERE DEPTNO = 55;

COMMIT;
SELECT * FROM DEPT;
ROLLBACK;


















